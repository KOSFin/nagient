# Webhook Transport

Use `webhook.sendNotification` for system notices and `webhook.sendMessage` for machine-readable replies.
Inbound webhook payloads should be normalized before reaching the agent runtime.
