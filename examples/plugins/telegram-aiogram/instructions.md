# Telegram (aiogram)

This is an optional aiogram-based Telegram transport example. Install it from
its Git repository with `nagient plugin install`; dependencies are installed in
the plugin's private `.venv`.

Set `proxy_url` when Telegram must use an HTTP, HTTPS, or SOCKS proxy.
